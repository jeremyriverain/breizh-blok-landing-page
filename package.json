{
  "name": "breizh-blok-landing-page",
  "version": "1.0.0",
  "type": "module",
  "private": "true",
  "scripts": {
    "dev": "pnpm --filter app dev",
    "generate": "pnpm --filter app generate",
    "preview": "pnpm -filter app preview"
  },
  "engines": {
    "node": "^22.0.0"
  }
}
